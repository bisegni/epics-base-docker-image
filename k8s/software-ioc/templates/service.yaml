apiVersion: v1
kind: Service
metadata:
  name: {{ include "software-ioc.fullname" . }}
  labels:
    {{- include "software-ioc.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "software-ioc.selectorLabels" . | nindent 4 }}
