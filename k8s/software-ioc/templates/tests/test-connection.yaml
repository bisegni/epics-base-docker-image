apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "software-ioc.fullname" . }}-test-connection"
  labels:
    {{- include "software-ioc.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "software-ioc.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
